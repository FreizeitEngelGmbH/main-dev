/**
 * Activity category groups used by the search page's category filter. The definition lives in the
 * source app's components/layout/categories-navbar-grouped.tsx (migrated unchanged); re-exported here
 * so the search page keeps its data import and there is a single definition.
 */
export { categoryGroups } from "@/components/layout/categories-navbar-grouped";
